#!/usr/bin/env bash
# Placeholder READY-stage init hook — intentionally a no-op. IAM seeding for
# the cloud layer lives in seed-iam.py (a Python hook, not shell — see its
# module docstring for why) alongside this file in the same ready.d mount.
